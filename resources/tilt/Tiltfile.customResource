# The after_all() function (if exists) will be called after main Tiltfile is executed in localdev
# def after_all():
#    k8s_resource(
#        workload="myservice", resource_deps=["myconfiguration"]
#    )
#    k8s_resource(
#        workload="myfrontend", resource_deps=["mybackend"]
#    )
