${id}-oauth-user-agent:
  type: oauthApplicationUserAgent
  scopes:
    - C_${Object}.everything

${id}-object-action:
  type: objectAction
  resourcePath: ${resourcePath}
  oAuth2ApplicationExternalReferenceCode: ${id}-oauth-user-agent