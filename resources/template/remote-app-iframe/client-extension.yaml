${id}:
    name: ${name}
    type: iframe
    url: https://arnab-datta.github.io/counter-app

assemble:
    - from: assets/
      include: "**/*"
      into: static/

runtime:
    template: remote-app-iframe
    type: static