assemble:
    - from: assets
      include: "**/*"
      into: static
${id}:
    cssURLs:
        - style.css
    friendlyURLMapping: ${id}
    htmlElementName: ${id}
    instanceable: false
    name: ${name}
    portletCategoryName: category.remote-apps
    type: customElement
    urls:
        - index.js
    useESM: false